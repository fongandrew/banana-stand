# Update .gitignore for Test Temp Directory

Add `.tmp/` to `.gitignore` so test temporary files are not committed.

## Requirements

1. Add `.tmp/` entry to the existing `.gitignore` file at repo root

## Acceptance Criteria

- [ ] `.gitignore` contains `.tmp/` entry
